spring:
  application:
    name: PostProject
#  jdbc
  datasource:
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
    driver-class-name: ${DATABASE_DRIVERCLASSNAME}
    url: ${DATABASE_URL}

#  jpa
  jpa:
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

server:
  port: 8080

logging:
  config: classpath:logback-spring-dev.xml
  level:
    org:
      hibernate:
        SQL: DEBUG


jwt:
  secret-key-source: GYeDVHHKWpLG4xH+WMn4C0Dmze6kNM1D0UBXlDSfF3pQ2dMW4wYpgu4t4nDvXJO7tx0IbPj7wxTky/GdxN3+Rg==